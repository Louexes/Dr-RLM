# <experiment-name>

**Question:** <the one thing this experiment answers>

**Setup:** jobs in `jobs/`; agent model = <…>; subset = `data/subsets/<…>`; config (max_depth / orchestrator
/ tool budget / shards) = <…>; outputs in `drrlm/runs/<ARM>/`.

**Result:** <headline number(s), with CI / N>

**Caveats:** <confounds, drops, things to re-check before trusting>

**Status:** planned | running | partial | done

<!-- Copy this dir to start a new experiment, then add a row to ../REGISTRY.md -->
