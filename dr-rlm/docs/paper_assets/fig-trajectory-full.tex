% ============================================================================
% FULL RAW-TRAJECTORY FIGURE (PLACEHOLDER) -- one coherent two-column example.
% Anatomy of a Dr-RLM trajectory on instance 24 (construction product from air-filter
% waste), v1-clean provenance-smoke RL run. Chosen because it shows the CLEAREST visible
% before/after: an early rollout where every sub-agent returns EMPTY and the root fabricates
% an ungrounded, uncited report; vs a late rollout where the sub-agents ground their findings
% and the root produces a cited feasibility report.
%
%   QUERY -> ROOT decomposes the brief into sub-questions -> delegates to sub-agents
%         -> ROOT synthesizes the final report
%
%   BEFORE: sub-agents all (empty), 0 cites -> root report is an invented product, 0 citations.
%   AFTER : sub-agents ground their findings (own-evidence cites 3/3/1/5, recorded in the run
%           ledger; their FINAL summaries drop the <cite> tags so we show the counts) ->
%           root report is grounded, 4 citations.
%
% TECHNICAL NOTE (verified vs node rids): the report's 4 citation ids all decode to d42178f8 =
% the ROOT's own retrieval, NOT the sub-agents. Per user (2026-07-01) the DISPLAYED caption is
% simplified to state only the citation count and that the root synthesizes the sub-agents'
% outputs -- it does NOT surface the owner=root detail (which is subtle and placeholder-only).
% Keep this in mind when regenerating: on the trained checkpoint the GOAL is for the root to
% actually cite the sub-agents' evidence, so this simplification becomes literally true.
% Sub-agent cite counts come from the ledger (n_cited); generate.py --log-dir will show inline
% child cites. Verbatim model text; whitespace normalized; [...] = elision. Sub-question wording
% is reconstructed (RL logs dump only final answers, not the delegation text); per user the
% on-figure "reconstructed" disclaimer was removed -- restore verbatim delegation on regeneration.
% Source: runs/provenance_smoke/credit_metrics_L3_v1clean.jsonl.
%
% >>> PLACEHOLDER: regenerate from the FINAL SFT+RL checkpoint before submission;
%     on the trained model, pick an OPEN query that ALSO shows root-invented decomposition. <<<
%
% Full-width (figure*). Needs \usepackage{tikz}, \usetikzlibrary{arrows.meta}.
% ============================================================================
\definecolor{cI}{HTML}{1F6FB2}\definecolor{cII}{HTML}{1B8A6B}%
\definecolor{cIII}{HTML}{C97A12}\definecolor{cBad}{HTML}{B03A2E}%
\providecommand{\cit}[2]{\textcolor{cIII}{#2}\,\raisebox{0.3ex}{\fontsize{5.6}{6}\selectfont\textcolor{cIII!85!black}{[\texttt{#1}]}}}
% citation-count chips: green = grounded (N ledger cites), red = none
\providecommand{\ncg}[1]{\tikz[baseline=-0.55ex]\node[fill=cII!12,text=cII!60!black,rounded corners=1.5pt,inner xsep=2.5pt,inner ysep=0.8pt,font=\fontsize{6}{7}\selectfont]{#1 cited};}
\providecommand{\ncr}{\tikz[baseline=-0.55ex]\node[fill=cBad!10,text=cBad!65!black,rounded corners=1.5pt,inner xsep=2.5pt,inner ysep=0.8pt,font=\fontsize{6}{7}\selectfont]{0 cited};}

\begin{figure*}[t]
\centering
\resizebox{\textwidth}{!}{%
\begin{tikzpicture}[font=\small,>=Latex,
  stage/.style={draw,rounded corners=3pt,line width=0.8pt,inner sep=6pt,align=left},
  flow/.style={-{Latex[length=6pt]},line width=1.1pt,black!45},
  flab/.style={font=\scriptsize\itshape,text=black!55,fill=white,inner sep=1.5pt}]

\def\W{16.4} \def\H{7.85} \def\xR{8.55} \def\xC{8.15}
\def\yD{-2.5} \def\yS{-5.3} \def\yR{-11.0}
\newcommand{\bxh}[2]{{\footnotesize\bfseries\textcolor{#1}{#2}}}
\newcommand{\bxc}[1]{{\scriptsize\color{black!55}#1}}

% ===================== QUERY (full width) =====================
\node[stage,fill=black!4,draw=black!35,text width=\W cm,anchor=north west] (Q) at (0,0){%
  {\footnotesize\bfseries\textsf{User query}}~~\bxc{a sprawling multi-part brief --- abridged}\\[3pt]
  ``[\,Waste:\,] Air filter. Design a prototype for construction. What product might be most feasible and
  in demand for this waste? What technical requirements must it meet per building regulations? What
  techniques integrate the waste? [\,\ldots\,] Are there successful architectural projects that used this
  waste? [\,\ldots\,] What is the production process? [\,\ldots\,]''};

% ===================== ROOT decomposition (full width) =====================
\node[stage,fill=cI!5,draw=cI,text width=\W cm,anchor=north west] (D) at (0,\yD){%
  \bxh{cI}{Root $\pi$}~\bxc{--- decomposes the multi-part brief, delegates four sub-questions to fresh-context sub-agents}\\[3pt]
  $a_1$~``What is the waste's material composition \& uses?'' \;$\cdot$\; $a_2$~``Which industrial waste types (fly ash, baghouse dust)?''\\[2pt]
  $a_3$~``What building-regulation / structural-safety limits apply?'' \;$\cdot$\; $a_4$~``What precedent reuse projects exist?''};

% ===================== SUB-AGENTS (before | after) : RAW OUTPUT =====================
\node[stage,fill=cBad!4,draw=cBad!70,anchor=north west] (SB) at (0,\yS){%
  \parbox[t][4.9cm][t]{7.4cm}{\footnotesize%
  \bxh{cBad}{BEFORE --- Sub-agents}~\bxc{early rollout, raw output}\\[6pt]
  $a_1$~\ncr~~\textcolor{cBad!80!black}{(empty)}\\[7pt]
  $a_2$~\ncr~~\textcolor{cBad!80!black}{(empty)}\\[7pt]
  $a_3$~\ncr~~\textcolor{cBad!80!black}{(empty)}\\[7pt]
  $a_4$~\ncr~~\textcolor{cBad!80!black}{(empty)}\\[8pt]
  \bxc{every delegated worker returns nothing --- no external evidence for the root to build on}}};
\node[stage,fill=cII!5,draw=cII,anchor=north west] (SA) at (\xR,\yS){%
  \parbox[t][4.9cm][t]{7.4cm}{\footnotesize%
  \bxh{cII}{AFTER --- Sub-agents}~\bxc{late rollout, raw output}\\[6pt]
  $a_1$~\ncg{3}~ ``\textbf{Material Composition.} Filters generate mixed-waste streams; media is typically \textbf{polypropylene (PP)} [\,\ldots\,]''\\[5pt]
  $a_2$~\ncg{3}~ ``Waste (\textbf{fly ash}, baghouse dust) is a viable raw material for construction bricks, tiles and adobe [\,\ldots\,]''\\[5pt]
  $a_3$~\ncg{1}~ ``\textbf{Regulations \& Safety.} Building codes are a complex landscape for mixed waste in structural use [\,\ldots\,]''\\[5pt]
  $a_4$~\ncg{5}~ ``\textbf{Precedent Projects.} Research found \textbf{no verified case studies} in the literature [\,\ldots\,]''}};

% ===================== FINAL REPORT (before | after) : RAW OUTPUT =====================
\node[stage,fill=cBad!4,draw=cBad!70,text width=\H cm,anchor=north west] (RB) at (0,\yR){%
  \bxh{cBad}{BEFORE --- Root final report}~\bxc{raw output}\\[3pt]
  ``\textbf{Prototype Design: ``Aero-Skin'' Adaptive Facade Panel.} The most technically feasible and
  market-demanding product [\,\ldots\,] is the \textbf{Aero-Skin Panel} [\,\ldots\,]''\\[3pt]
  \bxc{an invented product --- \textbf{0 citations}, nothing grounded}};
\node[stage,fill=cII!5,draw=cII,text width=\H cm,anchor=north west] (RA) at (\xR,\yR){%
  \bxh{cII}{AFTER --- Root final report}~\bxc{raw output}\\[3pt]
  ``\textbf{Research Report: Feasibility Study for Air Filter Waste} [\,\ldots\,]
  \cit{d42178f8-24}{Construction dust [\,\ldots\,] contains silica, a known carcinogen} [\,\ldots\,]
  \cit{d42178f8-23}{Construction waste recycling [\,\ldots\,] collecting, sorting and reusing materials} [\,\ldots\,]''\\[3pt]
  \bxc{grounded feasibility report --- \textbf{4 citations}, synthesized from the sub-agents' outputs}};

% ===================== flow arrows =====================
\draw[flow] (Q.south) -- (D.north);
\draw[flow] ([xshift=-4.0cm]D.south) -- (SB.north) node[flab,midway]{delegates};
\draw[flow] ([xshift= 4.0cm]D.south) -- (SA.north) node[flab,midway]{delegates};
\draw[flow] (SB.south) -- (RB.north);
\draw[flow] (SA.south) -- (RA.north);

% faint divider between before/after columns
\draw[black!12,line width=0.6pt] (\xC,\yS+0.15) -- (\xC,-13.65);

\end{tikzpicture}}
\caption{\textbf{One Dr-RLM trajectory, before vs.\ after training (placeholder, v1-clean smoke run).}
Raw model output at each stage; the \textcolor{cBad}{left} column is an early (untrained) rollout, the
\textcolor{cII}{right} a late (trained) one on the same brief. \emph{Before}, every sub-agent returns
empty (0 cites) and the root fabricates an ungrounded ``Aero-Skin'' product with no citations.
\emph{After}, each sub-agent grounds its findings (own-evidence cites $3/3/1/5$, recorded in the run
ledger) and the root synthesizes them into a feasibility report with \textbf{4 citations}
(\textcolor{cIII}{orange}). The scalar reward barely moves ($R\,0.80\!\rightarrow\!0.90$):
the rubric judge rewards the fluent early hallucination almost as much as the grounded report---it is
grounding-blind, which is precisely why per-node provenance credit is needed.
\textit{Final version uses the SFT$+$RL checkpoint with full-trajectory logging (children's inline
citations visible) and, ideally, an open query that also shows the root inventing its decomposition.}}
\label{fig:trajectory-full}
\end{figure*}
