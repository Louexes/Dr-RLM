#!/bin/bash
# Finish-up for 24413508: the 200 flat teacher trajectories generated fine
# (runs/sft_gen_flat/researchqa.jsonl); only the convert+filter crashed (judge.py
# file-import missing sys.modules registration — fixed). Needs just the 4B judge.
#SBATCH --job-name=sft_flat_conv
#SBATCH --partition=gpu_h100
#SBATCH --gpus=1
#SBATCH --cpus-per-task=16
#SBATCH --time=01:30:00
#SBATCH --output=/gpfs/home5/lgehringer/Dr-RLM/dr-rlm/runs/logs/sft_flatconv_%j.out
#SBATCH --error=/gpfs/home5/lgehringer/Dr-RLM/dr-rlm/runs/logs/sft_flatconv_%j.err
set -euo pipefail

REPO=/gpfs/home5/lgehringer/Dr-RLM; DRRLM=$REPO/dr-rlm
VENV=/gpfs/home5/lgehringer/venvs/rlm_vllm311_headers
JUDGE=gpt-4.1-mini-2025-04-14; JPORT=8100
OUT=$DRRLM/runs/sft_gen_flat; SFTD=$DRRLM/data/sft
FRAMING="Write a comprehensive, well-structured research report that fully answers the question. Support every claim with in-line citations to the retrieved snippets."
JOBID=${SLURM_JOB_ID:-local}
source "$VENV/bin/activate"
export TOKENIZERS_PARALLELISM=false JUDGE_API_KEY=EMPTY VLLM_USE_DEEP_GEMM=0 HF_HUB_OFFLINE=1

[ -s "$OUT/researchqa.jsonl" ] || { echo "[FATAL] generated rows missing"; exit 1; }

CUR_PID=""; cleanup(){ [ -n "$CUR_PID" ] && kill "$CUR_PID" 2>/dev/null || true; }; trap cleanup EXIT
vllm serve Qwen/Qwen3.5-4B --host 0.0.0.0 --port $JPORT --tensor-parallel-size 1 \
  --gpu-memory-utilization 0.90 --max-model-len 32768 --max-num-seqs 32 \
  --default-chat-template-kwargs '{"enable_thinking": false}' \
  --served-model-name "$JUDGE" > "$DRRLM/runs/logs/sft_flatconv_vllm_${JOBID}.log" 2>&1 &
CUR_PID=$!
for s in $(seq 1 120); do
  curl -sf "http://localhost:$JPORT/v1/models" >/dev/null 2>&1 && { echo "[serve] ready"; break; }
  sleep 5; kill -0 "$CUR_PID" 2>/dev/null || { echo "[FATAL] vllm died"; exit 1; }
done

python "$DRRLM/scripts/flat_traces_to_sft.py" --in "$OUT/researchqa.jsonl" \
  --out "$SFTD/dr_tulu_flat_sft.jsonl" --rubrics "$SFTD/sft_prompts_rubrics.jsonl" \
  --framing "$FRAMING" --judge_base_url "http://localhost:$JPORT/v1" --judge_model "$JUDGE" \
  --threshold 0.6 --tokenizer Qwen/Qwen3.5-4B --max_tokens 32768
echo "[step1-flat convert] complete -> $SFTD/dr_tulu_flat_sft.jsonl"
